# PostgreSQL connection string.
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
#
# Example (default used if DATABASE_URL is not set):
# DATABASE_URL=postgresql://postgres:mypassword@localhost:5432/mydatabase
#
# Create the database and user if needed:
#   createdb mydatabase
#   psql -c "ALTER USER postgres PASSWORD 'mypassword';"
DATABASE_URL=postgresql://postgres:mypassword@localhost:5432/mydatabase
